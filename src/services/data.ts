export type CaseItem = { id: string; title: string; status: string };
export type EvidenceItem = { id: string; name: string };
export type WitnessItem = { id: string; name: string };

export const cases: CaseItem[] = [];
export const evidences: EvidenceItem[] = [];
export const witnesses: WitnessItem[] = [];

